import React, { FunctionComponent } from 'react';
import { Button } from '@alifd/next';

const NextButton: FunctionComponent = () => <Button type="primary">HHHHH</Button>;

export default NextButton;
